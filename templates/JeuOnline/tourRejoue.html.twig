{% extends "Layouts/base.html.twig" %}

{% block title %}
    Partie en cours
{% endblock %}

{% block body %}

    <h1> jeu Local </h1>
    {% if app.session.get('newRound') == 1 %}
        {{ app.session.set('newRound', 0) }}
        Nouveau round!
    {% endif %}
    Round n° {{ app.session.get('nbRound') }}<br><br>

    {% if app.session.get('replay') == 1 %}

    {# Dans le cas ou la partie est déjà lancée, on affiche le joueur a qui c'est le tour #}
    Au tour de : {{ app.session.get('idJoueur') }} de relancer ses deux dés

    <br>
    <br>

    {% elseif app.session.get('replay') == 0 %}

lll : {{ idJoueur }}<br>
efzifneizfez
    {% endif %}
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>

    {% if resultat is defined %}
        Resultat des lancers : {{ resultat }}
        <br>
        Voulez vous rejouer lorsque ce tour sera terminé ?

        {% set urlOui = path('jeu_local_choix', {'choix' : 1}) %}
        <a href={{ urlOui }}>
        <button type="button" class="btn btn-primary">Oui</button>
        </a>
        {% set urlNon = path('jeu_local_choix', {'choix' : 0}) %}
        <a href={{ urlNon }}>
        <button type="button" class="btn btn-primary">Non</button>
        </a>
    {% endif %}
    <br>
    <br>

    {% if app.session.get('show') == 1 %}
       {{ app.session.set('show', 0) }}
            {% set urlLancerDes = path('jeu_local_lancer', {'idJoueur' : app.session.get('idJoueur')}) %}
            <a href = {{ urlLancerDes }}>
                <button type="button" class="btn btn-primary">Lancer les dés</button>
            </a>
    {% endif %}


{% endblock %}
